# Cloud Build configuration for API deployment only
steps:
  # Build API Image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'Dockerfile.api', '-t', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/juno-marketplace/api:${SHORT_SHA}', '.']

  # Push image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/juno-marketplace/api:${SHORT_SHA}']

  # Deploy API to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'api'
      - '--image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/juno-marketplace/api:${SHORT_SHA}'
      - '--platform=managed'
      - '--region=${_REGION}'
      - '--allow-unauthenticated'
      - '--memory=512Mi'
      - '--set-env-vars'
      - 'MONGODB_URI=mongodb+srv://paulmccarthy_db_user:DXWMTEVQUrL5gad3@junomarketplace.ci9sfz3.mongodb.net?retryWrites=true&w=majority,JUNO_APPLICATION_ID=PaulsMarketplace-cafd2e7e,JUNO_SECRET_KEY=fd4b6008-f8c5-4c76-beae-8279bac9a91c,SESSION_SECRET=DfkdSLDQRvk9vHdJFiEyGLNGSSD7x+6OkzcB4PQZNYU=,NODE_ENV=production,CORS_ORIGINS=https://frontend-xio7lz2d5a-uc.a.run.app,https://juno-marketplace-admin.netlify.app,https://frabjous-muffin-21b429.netlify.app,JUNO_REDIRECT_URI=https://api-xio7lz2d5a-uc.a.run.app/auth/junopay/callback,JUNOPAY_AUTHORIZE_URL=https://stg.junomoney.org/oauth/authorize,JUNOPAY_TOKEN_URL=https://stg.junomoney.org/oauth/token,JUNOPAY_API_BASE_URL=https://stg.junomoney.org/restapi'

substitutions:
  _REGION: us-central1

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: '1200s'